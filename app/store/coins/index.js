import coinsReducer from './reducer';
import coinsMiddleware from './middleware';
import * as actions from './actions';
import * as selectors from './selectors';

export { actions, selectors, coinsReducer, coinsMiddleware };
